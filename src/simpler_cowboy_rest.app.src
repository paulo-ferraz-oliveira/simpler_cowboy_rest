{application, simpler_cowboy_rest, [
    {applications, [
        % OTP dependencies
        kernel,
        sasl,
        stdlib,
        % External dependencies
        cowboy
    ]},
    {description, "A simpler, opinionated, `cowboy_rest` application-behaviour"},
    {licenses, ["MIT"]},
    {links, [{"GitHub", "https://github.com/paulo-ferraz-oliveira/simpler_cowboy_rest"}]},
    {vsn, semver},
    {env, [{shared_impl, undefined}]}
]}.
